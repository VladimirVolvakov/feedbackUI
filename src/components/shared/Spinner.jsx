import spinner from '../assets/spinner.jpg'

const Spinner = () => {
    return <img src={spinner} alt='Data is loading...' style={{ width: '100px', margin: 'auto', display: 'block' }} />
}

export default Spinner